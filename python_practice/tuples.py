#tuples are like lists but you cant add or remove anything from them
#tuples are defined with () instead of []
#only methods are count and index, magic methods hace __method name__

numbers = ( 1, 2, 3 )